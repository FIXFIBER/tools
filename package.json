{
  "name": "tools",
  "version": "1.0.0",
  "scripts": {
    "dev": "your-dev-command",
    "build": "npm run build",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}